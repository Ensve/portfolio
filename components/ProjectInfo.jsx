const ProjectInfo = ({ project }) => {
    // Use the project data to populate your component
    return (
        <div /* ... */>
            <h1>{project.title}</h1>
            {/* Other project details */}
        </div>
    );
};

export default ProjectInfo;